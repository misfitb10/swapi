import React from 'react';
// import styled from 'styled-components';

// TODO: Make searchbar a component (reusable etc.) because now it's in PersonOverview
const SearchBar = () => {
    console.log('Searchbar initiated');
};

export default SearchBar;
